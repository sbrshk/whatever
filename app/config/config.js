/*const Config = require('electron-config')

const config = new Config({
    defaults: {
        autoStart: false,
        backgroundMode: true,
        colorTheme: "classic"
    }
})

module.exports.config = config*/
